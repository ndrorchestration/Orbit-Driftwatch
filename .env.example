# Server-side only. Never expose this value through browser code or VITE_/NEXT_PUBLIC_ variables.
OPENAI_API_KEY=

# Optional server-side model override. The implementation defaults to gpt-5.6-luna.
OPENAI_MODEL=gpt-5.6-luna
