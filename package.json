{
  "name": "orbit-driftwatch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Observable multi-agent reasoning showcase",
  "scripts": {
    "test": "node --check src/app.js && node --check src/claim-audit-app.js && node --test",
    "verify": "npm test",
    "verify:deployment": "node scripts/verify-deployment.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
